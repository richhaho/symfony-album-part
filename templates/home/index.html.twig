{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    The database contains:
    <ul>
        <li><code><a href="{{ path('app_album_index') }}">{{ albums|length }} ALBUMS</a></code></li>
        <li><code><a href="{{ path('app_review_index') }}">{{ reviews|length }} REVIEWS</a></code></li>
        <li><code><a href="{{ path('app_comment_index') }}">{{ comments|length }} COMMENTS</a></code></li>
    </ul>
</div>
{% endblock %}
